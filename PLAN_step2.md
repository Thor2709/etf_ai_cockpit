# Canonical implementation programme

The current entry point is `AGENTS.md` → `docs/development/CONTROL_PLANE.md` →
`python scripts/programme_execution.py --check` → the selected `--issue` card.

Semantic product requirements remain in immutable source packages and canonical
extensions. `issues/programme_control_state.json` is the reviewed status/phase/
edge-evidence edit source. The atomic generator renders the registry, readiness,
phase order and status views. `DELIVERY_WORKFLOW.md` defines integration and
validation; historical B00–B13 groupings do not grant batching or write authority.

The original integration blueprint is preserved unchanged at
`plans/archive/2026-09-08-control-plane/PLAN_step2.md`. Do not resume its historical
active issue, root model, worktree claims or infrastructure sequence.
