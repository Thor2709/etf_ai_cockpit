# Archived completion-package sources

These nine files are immutable archive copies of the validated external ZIP. The ZIP itself is deliberately not committed. See `SOURCE_MANIFEST.sha256` for the package and member SHA-256 values.
