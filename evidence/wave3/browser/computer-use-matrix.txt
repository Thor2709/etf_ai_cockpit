Computer-use browser matrix - 2026-07-10

Target: Chrome window 1183296, local packaged app URL http://127.0.0.1:8583/
Method: Windows Computer Use screenshots and coordinate interaction. Flet canvas text was not exposed as a dependable semantic accessibility tree, so these checks are visual and address-bar based.

PASS - Main page rendered after the packaged app settled. The first viewport showed Simple Scores, 59 instruments, workflow buttons and the Primary tier - ETFs heading.
PASS - Row expansion: the VWCE row expanded and displayed evidence, quality, risk/friction, model-authority, calibration, backtest and history/detail chips.
PASS - Primary tier - stocks/equity certificates rendered with stock rows.
PASS - Secondary tier - ETFs and Secondary tier - stocks/equity certificates rendered as separate sections.
PASS - Sparebanken rendered as a distinct section with 15 instruments. AURG, SOGN, MELG, SADG and SKUE visibly showed ISIN needs_verification. Known ISIN rows remained populated.
PASS - Sparebanken lower rows rendered HELG, JAEREN, SADG, SKUE, NONG, RING, MING, SOAG, SPOL, MORG and SPOG; unavailable score state for JAEREN/SADG was shown as N/A/Manual Review rather than invented data.
PASS - Provider Status rendered conservative unavailable provider states and probe messages.
PASS - Evidence Ledger rendered score components, freshness status and score eligibility columns.
PASS - Filings & Statements rendered SEC/ESEF import buttons and an explicit empty/missing inventory state.
PASS - ETF Disclosures rendered PRIIPs KID and index-methodology import buttons and an explicit inventory.
PASS - News & Context rendered explicit missing news and point-in-time validation states.
PASS - Diagnostics rendered packaged executable path, runtime versions, optional model availability and session events.
PASS - Errors & Recovery rendered deterministic provider timeout records with retry controls.
PASS - Data Health rendered healthy, missing and unavailable store statuses. Clicking Export health CSV produced a visible success message and data/derived/data_health.csv.
PASS - Universe rendered tier, asset type, Yahoo symbol and verified/needs_verification ISIN states. The lower table showed the Sparebanken records separated from secondary rows.
PASS - First-run Setup, What Changed, Instrument Detail and Import & Export routes rendered without a crash. Import & Export buttons remained visible; Backup and Restore did not produce a misleading success message.
PASS - Audit Notes rendered the export/import controls. Clicking Export audit packet produced a visible exported path; navigating to Diagnostics in the same source session then showed the completed export event and output path in the Session log.
PARTIAL - Show scores workflow button was clicked and the page remained stable; this button did not visibly change route in the packaged app.
LIMITATION - Direct reload to / initially showed the loader/blank viewport for about 5 seconds before the Flet page rendered. This is recorded as a startup timing limitation, not a failed HTTP readiness check.
LIMITATION - Playwright/in-app browser semantic assertions were not used as a pass claim because the Flet canvas transport exposed insufficient text locators; the in-app browser refusal is recorded separately.

Computer-use screenshot evidence was displayed during the run. The saved Chrome screenshot files under this directory are supplementary captures; the authoritative visual observations above come from the displayed Windows capture states.
