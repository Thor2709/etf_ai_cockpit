# ISSUE-0069 UI gate

Windows Computer Use rendered `/chatgpt` and clicked `Export audit packet`; the page displayed the concrete exported path. The same source session rendered `/diagnostics` and showed the recent `activity_complete | success | Export audit packet` event with the output path, plus the session ID, log path and redaction note.

Visual evidence is recorded in `evidence/wave3/browser/computer-use-matrix.txt`.
