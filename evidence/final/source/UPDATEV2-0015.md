# UPDATEV2-0015 source gate

The ETF document registry is implemented in `src/etf_cockpit/data/fund_documents.py` and exposed through `src/etf_cockpit/app/pages/trust_evidence.py` and the Instrument Detail selector. It keeps document type, source identifier, authority, date, checksum and explicit missing/invalid states. ETF disclosure inventory is evidence-only; it cannot alter score authority or enable execution. `execution_allowed` remains `false`.

The package evidence below is a clean native rebuild of the current reviewed tree. The local fixture inventory contains a real downloaded Vanguard issuer holdings workbook (`evidence/final/fixtures/Vanguard_VWCE_holdings.xlsx`) and the derived import CSV; the issuer page URL, retrieval date, holdings date and checksums are recorded in the fixture evidence. The imported VWCE store contains 3,789 rows, retains the observed 97.8728% total as `partial`, and is explicitly `score_eligible=false` because partial issuer holdings remain context-only. The audit ZIP includes `evidence_export/fund_holdings.csv` and its JSON representation with checksums. Missing KID, factsheet and other documents remain explicit unavailable states rather than invented evidence.
