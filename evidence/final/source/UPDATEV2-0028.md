# UPDATEV2-0028 source gate

`export_pack.py` builds the combined review packet and writes the audit manifest after all members exist. `audit_packet.py` validates required paths, checksums and unlisted members. `import_audit.py` preserves external commentary as non-executable evidence. Optional providers and parser inventories are explicit unavailable records when no valid source is imported.
