# Active Codex goal

Complete the canonical programme through Phase 11 without enabling execution.
There is no hand-maintained current issue, main SHA or ownership table here.

Start with `AGENTS.md` and `docs/development/CONTROL_PLANE.md`, fetch main, then
run `python scripts/programme_execution.py --check`. Its on-demand report derives
NOW/NEXT from current canonical lifecycle/readiness and identifies historical
implementation worth inspecting before coding. Read the selected `--issue` card
and only the relevant source/tests. The report is not acceptance or dispatch
permission; reconcile local owners and fresh PR heads before admission.

Root-owned active lanes live outside tracked policy in the common Git directory,
`etf-control/active-work.json`, validated with current programme/main identity.
Do not append session transcripts here. Keep exact-head evidence in its existing
artifact location and report references in the lane handoff. Review/CI/status
completion remains governed by the delivery workflow.

The former 3,961-line record is preserved byte-for-byte in
`plans/archive/2026-09-08-control-plane/plans/ACTIVE_CODEX_GOAL.md`.
Historical assertions inside it are not current operating instructions.
